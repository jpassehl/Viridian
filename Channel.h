#ifndef _Channel
#define _Channel

#include "fmod.hpp"
#include "AzulCore.h"

class Channel
{
public:
	Channel();
	Channel(const char* const _soundfile);
	virtual ~Channel(){};

private:
		bool isActive;
		typedef FMOD::Channel* SoundChannel;
		SoundChannel soundChannel;
		void privSetChannelPos(Vect& vect);
public:
	////////////////////////////////////////////////////////////////////////////////////////////////////
	/// @defgroup Channel Channel
	///  @ingroup SoundSystem
	///	  @brief The Channel, used for both 2d and 3d sounds to show which channel they are linked too. This sound system
	///	  is initalized with 36 channels.
	////////////////////////////////////////////////////////////////////////////////////////////////////
	/** @{ */

		////////////////////////////////////////////////////////////////////////////////////////////////////
		/// <summary>	Sets channel position. </summary>
		///
		/// <param name="vect">	The vector. </param>
		////////////////////////////////////////////////////////////////////////////////////////////////////

		void SetChannelPosition(Vect& vect);

		bool getActive()
		{
			return isActive;
		}

		////////////////////////////////////////////////////////////////////////////////////////////////////
		/// <summary>	Gets the channel. </summary>
		///
		/// <returns>	The channel. </returns>
		////////////////////////////////////////////////////////////////////////////////////////////////////

		SoundChannel getChannel()
		{
			return soundChannel;
		}

		////////////////////////////////////////////////////////////////////////////////////////////////////
		/// <summary>	Sets channel to active or not active .  </summary>
		///
		/// <param name="act">	boolean active. </param>
		///
		/// <returns>	sets the active channel. </returns>
		////////////////////////////////////////////////////////////////////////////////////////////////////

		bool setActive(bool act)
		{
			return isActive = act;
		}
		/** @} */
};


#endif _Channel